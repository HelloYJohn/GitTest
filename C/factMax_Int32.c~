#include <stdio.h>

int main()
{
    int n = 1;
    int result = 1, temp;
    
    while(1)
    {
      temp = result;
      result = result * n;
      if(result < 0)
      {
	  break;
      }
      n++;
    }
    
}
